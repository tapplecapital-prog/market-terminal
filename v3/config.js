// Leave blank when the frontend is served by server.py or Render as one app.
// For GitHub Pages, set this to the public backend URL, for example:
// window.MARKET_API_BASE = "https://your-market-terminal-api.onrender.com";
window.MARKET_API_BASE = window.MARKET_API_BASE || "";
